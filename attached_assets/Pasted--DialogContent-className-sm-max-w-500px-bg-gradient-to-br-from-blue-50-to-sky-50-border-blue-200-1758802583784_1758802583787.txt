 <DialogContent className="sm:max-w-[500px] bg-gradient-to-br from-blue-50 to-sky-50 border-blue-200">
        <DialogHeader className="text-center space-y-3">
          <DialogTitle className="text-2xl font-bold text-gray-900">
            Coming Soon, Join the Waitlist
          </DialogTitle>
        </DialogHeader>

        <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-5 mt-6">
          <div className="space-y-2">
            <Label htmlFor="fullName" className="text-sm font-medium text-gray-700">
              Full Name
            </Label>
            <Input
              id="fullName"
              {...form.register('fullName')}
              placeholder="Your full name"
              className="border-blue-200 focus:border-blue-400 focus:ring-blue-400"
              data-testid="input-full-name"
            />
            {form.formState.errors.fullName && (
              <p className="text-sm text-red-600">{form.formState.errors.fullName.message}</p>
            )}
          </div>

          <div className="space-y-2">
            <Label htmlFor="email" className="text-sm font-medium text-gray-700">
              Email Address
            </Label>
            <div className="relative">
              <Input
                id="email"
                type="email"
                {...form.register('email')}
                placeholder="your.email@example.com"
                className="border-blue-200 focus:border-blue-400 focus:ring-blue-400 pl-10"
                data-testid="input-email"
              />
              <Mail className="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400 h-4 w-4" />
            </div>
            {form.formState.errors.email && (
              <p className="text-sm text-red-600">{form.formState.errors.email.message}</p>
            )}
          </div>

          <div className="space-y-2">
            <Label htmlFor="primaryPillar" className="text-sm font-medium text-gray-700">
              Primary Pillar You Want to Improve
            </Label>
            <Select value={form.watch('primaryPillar')} onValueChange={(value) => form.setValue('primaryPillar', value)}>
              <SelectTrigger className="border-blue-200 focus:border-blue-400 focus:ring-blue-400" data-testid="select-pillar">
                <SelectValue placeholder="Select your focus area" />
              </SelectTrigger>
              <SelectContent>
                {PILLAR_OPTIONS.map((pillar) => (
                  <SelectItem key={pillar.value} value={pillar.value}>
                    <div className="flex items-center gap-2">
                      <span>{pillar.icon}</span>
                      <div>
                        <div className="font-medium">{pillar.label}</div>
                        <div className="text-xs text-gray-500">{pillar.description}</div>
                      </div>
                    </div>
                  </SelectItem>
                ))}
              </SelectContent>
            </Select>
            {form.formState.errors.primaryPillar && (
              <p className="text-sm text-red-600">{form.formState.errors.primaryPillar.message}</p>
            )}
          </div>

          <div className="bg-gradient-to-r from-blue-500/10 to-sky-500/10 rounded-lg p-4 border border-blue-200">
            <div className="text-center">
              <Badge variant="secondary" className="bg-blue-100 text-blue-800 px-3 py-1 mb-2">
                <Sparkles className="h-3 w-3 mr-1" />
                Founding Member Exclusive
              </Badge>
              <h3 className="font-bold text-lg text-gray-900 mb-1">
                Claim 50% Off & Early Access
              </h3>
              <p className="text-sm text-gray-600">
                Founding members receive 50% off launch pricing. By joining, you agree to receive educational emails and updates. Unsubscribe anytime.
              </p>
            </div>
          </div>

          <Button
            type="submit"
            disabled={isSubmitting}
            className="w-full brand-gradient hover:opacity-90 text-white font-bold py-3 text-lg"
            data-testid="button-join-waitlist"
          >
            {isSubmitting ? "Joining..." : "Claim 50% Off & Early Access"}
          </Button>
        </form>
      </DialogContent>